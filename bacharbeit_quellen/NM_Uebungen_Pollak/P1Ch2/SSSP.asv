function [d,p,T] = SSSP(Adj,g,s,t)

% Algorithm1:SSSPInit (Skript S.44)

n = length(Adj);
d = inf(1,n);
p = zeros(1,n);

d(s) = 0;

% Ende Algorithm 1

%Algorithm3 - SSSPDijkstra



W=Adj;
%
while ~isempty(W)
    i = W(min(d(W))==d(W));
    W = W - i;
    for i=1:length(Adj)
        

end;
