function [C,CLin] = myEuler(I,g) % Von S. 17, Algo's 'Explore' und 'Euler'

% Init. v. Algo. 'Euler'
cur = ones(1,n);
C = ones(1,n);
CLin = ones(1,n - 1);
run = true;

while run == true
    while cur(i) <= g(1,i)
        % init. v. Algo. 'Explore'
        e=cur(i);
        u =  % omega(e)
    end;
end;

end